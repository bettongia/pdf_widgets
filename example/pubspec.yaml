name: example
description: "Desktop PDF viewer example for betto_pdf."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  betto_pdf_widgets:
    path: ..
  #betto_pdfium_ios: ^0.1.0-dev.2
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_fonts: ^6.2.1
  intl: ^0.20.2
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
